<%= render(
      "gobierto_people/people/item_teaser",
      title: invitation.organizer,
      subtitle: invitation.title,
      id: invitation.to_anchor,
      dates: [invitation.start_date, invitation.end_date]
    ) %>
